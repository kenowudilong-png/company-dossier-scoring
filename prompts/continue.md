# Continue Prompt

读取 `AGENTS.md`、`TASK.md`、`STATE.md` 和 `LOG.md`。

找到 `STATE.md` 中当前步骤，对照 `TASK.md` 执行下一项未完成任务。执行时遵守：

1. 如果当前阶段是 M0，只做验证和文档回填，不写业务代码。
2. 所有未明确的包名、API、端点、端口、模型名必须先查官方来源。
3. 每完成一个可见进展，更新 `STATE.md` 与 `LOG.md`。
4. 遇到证据不足或合规风险，记录 TODO 和阻塞原因，不要编造。
5. 除非 `TASK.md` 全部完成或遇到必须人工决策的阻塞，否则继续推进。
